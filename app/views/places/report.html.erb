<%= form_with model:@place, local: true do |form| %>
  
  
    <%= form.fields_for :products do |f| %>
    <div class="bg-gray-50 mt-4 py-4">
        <p class="text-base text-gray-500 px-4"><%= f.object.name %></p>
        
        <div class="flex flex-wrap">
            <div class="w-full md:w-1/4 px-4">
                <div class="">
                    <%= f.label "Mantenimiento Preventivo" %>
                    <%= f.check_box :working, class:"" %>
                </div>
            </div>

            <div class="w-full md:w-1/4 px-4">
                <div class="">
                    <%= f.label "Mantenimiento correctivo" %>
                    <%= f.check_box :working, class:"" %>
                </div>
            </div>

            <div class="w-full md:w-1/4 px-4">
                <div class="">
                    <%= f.label "Sustitución" %>
                    <%= f.check_box :working, class:"" %>
                </div>
            </div>

            <div class="w-full md:w-1/4 px-4">
                <div class="">
                    <%= f.label "Obrservaciones" %>
                    <%= f.text_area :annotation, rows:3, class:"appearance-none block w-full py-3 px-4 leading-tight text-white-700 bg-white-50 focus:bg-white border border-white-200 focus:border-white-500 rounded" %>
                </div>
            </div>
        </div>
    </div>
  <% end %>


  <div class="mt-4">
    <%= form.submit 'Generar reporte', class:"cursor-pointer inline-block  py-4 px-8 leading-none text-white bg-indigo-600 hover:bg-indigo-700 font-semibold rounded shadow" %>
  </div>

<% end %>